server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/thiagoporb/config-files
          #clone-on-start: true
          #username: thiagoporb
    bus:
      enabled: true
  kafka:
    bootstrap-servers: http://localhost:9092